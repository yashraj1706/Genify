// Sample Appwrite configuration. Copy this file to appwriteConfig.js and fill in real values.
// The real file appwriteConfig.js is ignored by git (see .gitignore).

export const awConfig = {
  endpoint: "https://cloud.appwrite.io/v1", // e.g. https://cloud.appwrite.io/v1
  platform: "com.your.bundle.id", // Your app bundle ID
  projectId: "YOUR_PROJECT_ID", // Appwrite Project ID
  databaseId: "YOUR_DATABASE_ID", // Appwrite Database ID
  userCollectionId: "YOUR_USER_COLLECTION_ID", // Users collection ID
  videoCollectionId: "YOUR_VIDEO_COLLECTION_ID", // Videos collection ID
  storageId: "YOUR_STORAGE_ID", // Storage bucket ID
};
